import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:smart-wizard-step', 'SmartWizardStepView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
