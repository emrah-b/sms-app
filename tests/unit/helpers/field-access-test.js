import {
  fieldAccess
} from 'kingmesaj/helpers/field-access';

module('FieldAccessHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = fieldAccess(42);
  ok(result);
});
