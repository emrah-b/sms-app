import {
  phoneNumber
} from 'kingmesaj/helpers/phone-number';

module('PhoneNumberHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = phoneNumber(42);
  ok(result);
});
