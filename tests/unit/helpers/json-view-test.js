import {
  jsonView
} from 'kingmesaj/helpers/json-view';

module('JsonViewHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = jsonView(42);
  ok(result);
});
