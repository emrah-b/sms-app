import {
  smsStatus
} from 'kingmesaj/helpers/sms-status';

module('SmsStatusHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = smsStatus(42);
  ok(result);
});
